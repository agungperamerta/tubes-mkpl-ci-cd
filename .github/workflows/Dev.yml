name: CI/CD Pipeline

on:
  push:
    branches:
      - dev
  pull_request:
<<<<<<< Updated upstream
    branches:
      - dev
=======
    branches: [main]
  release:
    types: [created]
>>>>>>> Stashed changes

jobs:
  # Job 1: CI - Jalan saat pull_request
  build-and-test:
    runs-on: ubuntu-latest
<<<<<<< Updated upstream

=======
    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK 8
      uses: actions/setup-java@v3
      with:
        java-version: '8'
        distribution: 'temurin'

    - name: Build with Maven
      run: mvn clean package

    - name: Run tests
      run: mvn test

  # Job 2: CD - Jalan saat release dibuat
  publish:
    runs-on: ubuntu-latest
    if: github.event_name == 'release'  # ← hanya jalan saat release
    permissions:
      contents: read
      packages: write
>>>>>>> Stashed changes
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
<<<<<<< Updated upstream
    
    - name: Build with Maven
      run: mvn clean package
    
    - name: Run tests
      run: mvn test
    
=======
        server-id: github
        settings-path: ${{ github.workspace }}

>>>>>>> Stashed changes
    - name: Publish to GitHub Packages
      run: mvn deploy
      env:
        GITHUB_TOKEN: ${{ github.token }}